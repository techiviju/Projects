:root {
  --primary: #0b5562;
  --accent: #e24b6a;
  --surface: #ffffff;
  --muted: #f7fafb;
  --shadow: 0 8px 24px rgba(11,85,98,0.08);
  --radius-md: 12px;
  --radius-lg: 18px;
  --transition: 200ms cubic-bezier(.2,.9,.3,1);
}

body {
  background: var(--muted);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto;
  color: #142b32;
}

/* Navbar */
.navbar {
  box-shadow: var(--shadow);
  background: linear-gradient(90deg, var(--primary), #0d8a8a);
  z-index: 1001;
  position: relative;
}

.navbar-brand {
  font-weight: 700;
  color: #fff !important;
}
.navbar-nav .nav-link {
  color: #fff !important;
  font-weight: 500;
}
.navbar-nav .nav-link:hover {
  color: #d9f0f3 !important;
}
/* Cards */
.card.my-card {
  border: none;
  border-radius: var(--radius-md);
  box-shadow: var(--shadow);
  transition: transform var(--transition), box-shadow var(--transition);
}
.card.my-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 12px 36px rgba(11,85,98,0.15);
}
/* Buttons */
.btn-primary, .btn-danger {
  border: none;
  border-radius: 10px;
  font-weight: 600;
  transition: transform var(--transition), box-shadow var(--transition);
}
.btn-danger {
  background: linear-gradient(90deg, var(--accent), #c32b4f);
}
.btn-primary {
  background: linear-gradient(90deg, var(--primary), #0d8a8a);
}
.btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 16px rgba(11,85,98,0.1);
}
/* Forms */
.form-control, .form-select {
  border-radius: 10px;
  border: 1px solid #e2ebec;
  transition: border-color var(--transition), box-shadow var(--transition);
}
.form-control:focus, .form-select:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 0.15rem rgba(11,85,98,.25);
}
/* Table */
.table {
  border-radius: var(--radius-md);
  overflow: hidden;
}
.table thead th {
  background: var(--primary);
  color: #fff;
  border: none;
}
.table-striped>tbody>tr:nth-of-type(odd) {
  background-color: #f2f8f9;
}
/* Headings */
.page-title {
  color: var(--primary);
  font-weight: 700;
}
/* Modal */
.modal-content {
  border-radius: var(--radius-md);
  box-shadow: var(--shadow);
}
