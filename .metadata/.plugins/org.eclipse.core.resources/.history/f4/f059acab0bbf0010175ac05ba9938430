:root {
  --primary: #0b5562;
  --accent: #e24b6a;
  --surface: #fff;
  --muted: #f6f9fb;
  --text: #102027;
  --card-shadow: 0 8px 24px rgba(11,85,98,0.08);
  --radius-lg: 14px;
  --radius-md: 8px;
  --transition: 200ms cubic-bezier(.2,.9,.3,1);
}
body {
  background: var(--muted);
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', Arial, sans-serif;
  color: var(--text);
  -webkit-font-smoothing: antialiased;
  line-height: 1.45;
}
.card.my-card {
  border: none;
  border-radius: var(--radius-lg);
  box-shadow: var(--card-shadow);
  transition: transform var(--transition), box-shadow var(--transition);
  overflow: hidden;
}
.card.my-card:hover {
  transform: translateY(-6px);
  box-shadow: 0 18px 40px rgba(11,85,98,0.12);
}
.btn.my-cta {
  background: linear-gradient(90deg, var(--primary), #0a7a78);
  border: none;
  color: #fff;
  border-radius: 10px;
  padding: 0.62rem 1.0rem;
  font-weight: 600;
  transition: transform var(--transition), box-shadow var(--transition);
}
.btn.my-cta:hover {
  transform: translateY(-2px);
  box-shadow: 0 10px 20px rgba(11,85,98,0.08);
}
.form-control, .form-select {
  border-radius: 10px;
  padding: .6rem .75rem;
  border: 1px solid #e6eef0;
  box-shadow: none;
  transition: border-color var(--transition), box-shadow var(--transition);
}
.form-control:focus, .form-select:focus {
  outline: none;
  border-color: var(--primary);
  box-shadow: 0 6px 18px rgba(11,85,98,0.06);
}
.table thead th {
  border-bottom: none;
  background: transparent;
  color: #24313a;
  font-weight: 700;
}
.table tbody td {
  vertical-align: middle;
  border-top: 1px solid #f0f5f6;
}
.center-block {
  margin-left: auto;
  margin-right: auto;
}
.round-avatar {
  border-radius: 50%;
  width: 44px;
  height: 44px;
  object-fit: cover;
}
.kicker {
  font-size: .85rem;
  color: #5b6b6e;
}
@media (max-width: 768px) {
  .card.my-card { border-radius: 12px; padding: .6rem; }
}
